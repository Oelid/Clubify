spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clubify
    username: clubify
    password: clubify
  docker:
    compose:
      enabled: true
      file: compose.yaml
springdoc:
  swagger-ui:
    enabled: true
    path: /swagger-ui
  api-docs:
    enabled: true
clubify:
  auth:
    # En développement, l'interface tourne en clair sur localhost : un cookie
    # « Secure » ne serait jamais renvoyé par le navigateur.
    cookie-secure: false
  security:
    jwt:
      secret: dev-secret-a-remplacer-par-une-variable-d-environnement-32o
  encryption:
    key: Y2x1YmlmeS1jbGUtZGUtZGV2LS0zMi1vY3RldHMhISE=
logging:
  level:
    ma.clubify: DEBUG
